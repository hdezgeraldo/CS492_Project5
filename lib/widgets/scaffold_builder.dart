import 'package:flutter/material.dart';

class ScaffoldBuilder extends StatelessWidget {
  const ScaffoldBuilder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
